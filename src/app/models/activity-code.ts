export interface ActivityCodeToReturnDto {
    id: number;
    code: string;
    description: string;
}
export interface AddActivityCodeDto {
    code: string;
    description: string;
}
export interface UpdateActivityCodeDto {
    code: string;
    description: string;
}